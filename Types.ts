export type Session = {
    AccessToken: string;
    ApiGatewayUri: string;
    ApiGatewayVersion: string;
}

export type Metadata = {
    date: string;
    title: string;
    playbackUrl: string;
    posterImage: string;
}